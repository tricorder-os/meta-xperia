# distribution config
DISTRO = "phonos"
DISTRO_NAME = "phonos xperia operating system"
DISTRO_VERSION = "${DATE}"

TARGET_VENDOR = "-sony"

IMAGE_FSTYPES ?= "tar.bz2 ext4"
PACKAGE_CLASSES = "package_deb"

GLIBC_GENERATE_LOCALES = "en_US.UTF-8"
IMAGE_LINGUAS = "en-us"

require conf/distro/include/distrofeatures.inc
